import React from 'react';

const CartPage: React.FC = () => {
  return (
    <div style={{ padding: 32, textAlign: 'center', fontSize: 20, color: '#888' }}>
      购物车页面占位
    </div>
  );
};

export default CartPage; 